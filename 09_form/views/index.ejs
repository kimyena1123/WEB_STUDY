<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%= title %></h1>
    <p><%= desc %></p>

    <h2>GET 요청</h2>

    <form action="/getForm" method = "get" name = "login1">
        <input id = "id" name = "id" type = "text" placeholder="ID" required minlength = "3">
        <input id = "pw" name = "pw" type = "password" placeholder="PASSWORD">
        <input type = "email" placeholder="EMAIL" required>
        <input type="number" placeholder="GRADE" required min = "3" max = "7">
        <button type = "submit">제출</button>
    </form>
    <br>

    <h2>POST 요청</h2>
    <form action="/postForm" method="post" name="login2">

        <!-- 아이디 조건 : 대소문자, 한글숫자, 4자이상 -->
        <!-- 비밀번호 조건 : 소문자 ,숫자로 8자리 이상 12자리 이하 -->
        <input id="id" name="id" type="text" placeholder="ID"
                required pattern="^([a-zA-Z0-9가-힣]){4,}$"
                title="아이디는 대소문자, 한글, 숫자, 4자 이상 작성해주세요">

        <input id="pw" name="pw" type="password" placeholder="PASSWORD"
                required pattern = "^([a-z0-9]){8,12}"
                title = "비밀번호는 소문자, 숫자로 8자리 이상 12자리 이하로 작성해주세요">

        <button type="submit">제출</button>
    </form>

    <a href="/prac24">prac24</a>
    <a href="/prac27">prac27</a>
    <script>
        //form 태그의 name 속성 값으로 폼 선택하기
        console.log('login1 : ' + document.forms['login1']);
        console.log('login2 : ' + document.forms['login2']);
    </script>
</body>
</html>