<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>댓글 모음</h1>
    
    <a href="/">홈으로</a>

    <!-- 전체 댓글(4개) 보이기 -->
    <ul>
        <% for(let i = 0; i< commentsInfo.length; i++) { %>
            <li><%= commentsInfo[i].userid %> - <a href='/comment/<%= i + 1 %>'><%= commentsInfo[i].comment %></a></li>
        <% } %>
    </ul>
</body>
</html>